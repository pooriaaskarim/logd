name: logd_benchmarks
description: Benchmarks for the logd package.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.5.0 <4.0.0'

dependencies:
  benchmark_harness: ^2.4.0
  logd:
    path: ../logd
  meta: ^1.11.0

dev_dependencies:
  lints: ^3.0.0
